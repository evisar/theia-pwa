import { Position, TextEdit, TextDocumentIdentifier } from 'vscode-languageserver-types';
export declare class DockerRename {
    rename(textDocument: TextDocumentIdentifier, content: string, position: Position, newName: string): TextEdit[];
}
