import { Bounds, Point } from '../../utils/geometry';
import { SParentElement, SChildElement } from "../../base/model/smodel";
import { AbstractLayout } from './abstract-layout';
import { AbstractLayoutOptions, VAlignment } from './layout-options';
import { BoundsData } from './hidden-bounds-updater';
import { LayoutContainer } from './model';
import { StatefulLayouter } from './layout';
export interface HBoxLayoutOptions extends AbstractLayoutOptions {
    hGap: number;
    vAlign: VAlignment;
}
/**
 * Layouts children of a container in horizontal (left->right) direction.
 */
export declare class HBoxLayouter extends AbstractLayout<HBoxLayoutOptions> {
    static KIND: string;
    protected getChildrenSize(container: SParentElement & LayoutContainer, containerOptions: HBoxLayoutOptions, layouter: StatefulLayouter): {
        width: number;
        height: number;
    };
    protected layoutChild(child: SChildElement, boundsData: BoundsData, bounds: Bounds, childOptions: HBoxLayoutOptions, containerOptions: HBoxLayoutOptions, currentOffset: Point, maxWidth: number, maxHeight: number): Point;
    protected getDefaultLayoutOptions(): HBoxLayoutOptions;
    protected spread(a: HBoxLayoutOptions, b: HBoxLayoutOptions): HBoxLayoutOptions;
}
//# sourceMappingURL=hbox-layout.d.ts.map