import { Point } from "../../utils/geometry";
import { SModelElement } from "../../base/model/smodel";
import { SModelExtension } from "../../base/model/smodel-extension";
export declare const moveFeature: unique symbol;
/**
 * An element that can be placed at a specific location using its position
 * property.
 */
export interface Locateable extends SModelExtension {
    position: Point;
}
export declare function isLocateable(element: SModelElement): element is SModelElement & Locateable;
export declare function isMoveable(element: SModelElement): element is SModelElement & Locateable;
//# sourceMappingURL=model.d.ts.map