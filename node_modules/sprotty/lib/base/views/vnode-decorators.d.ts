import { VNode } from "snabbdom/vnode";
import { SModelElement } from "../model/smodel";
/**
 * Manipulates a created VNode after it has been created.
 * Used to register listeners and add animations.
 */
export interface IVNodeDecorator {
    decorate(vnode: VNode, element: SModelElement): VNode;
    postUpdate(): void;
}
export declare class FocusFixDecorator implements IVNodeDecorator {
    static tabIndex: number;
    decorate(vnode: VNode, element: SModelElement): VNode;
    postUpdate(): void;
}
//# sourceMappingURL=vnode-decorators.d.ts.map